#ifndef NATIVELIB_CORE_H
#define NATIVELIB_CORE_H

#include <string>

// Function to process the input string (core logic)
std::string core_process_string(const std::string& input_str);

// Function to get the global string (core logic)
std::string core_get_global_string();

#endif // NATIVELIB_CORE_H 